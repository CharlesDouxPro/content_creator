# openapi-typescript@7 déclare un peer `typescript@^5`, mais le scaffold Vite installe
# TypeScript 6 (compatible en pratique pour la génération de types). On accepte ce peer
# plutôt que d'échouer : `npm install` fonctionne sans flag.
legacy-peer-deps=true
